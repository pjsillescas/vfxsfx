#version 330 core

layout (location = 0) in vec3 LVertexPos;
layout (location = 1) in vec2 texCoor;
layout (location = 2) in vec3 ColorVertex;

out vec2 texCoord;

void main()
{
	gl_Position = vec4(LVertexPos.xyz, 1);
	texCoord = texCoor;
}
