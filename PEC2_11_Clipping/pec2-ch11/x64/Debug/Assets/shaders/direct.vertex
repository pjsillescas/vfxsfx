#version 330 core

layout (location = 0) in vec2 LVertexPos2D;
out vec4 gl;

void main(){

	if (LVertexPos2D.x > 0) 
		gl = vec4 (1.0,0,0,1.0);
	else
		gl = vec4(0,0,1.0,1.0);
	
	gl_Position = vec4( LVertexPos2D.x, LVertexPos2D.y, 0, 1 );
}
